iframe : target for a link.


task 1: actually I'm trying to give margin to my entire table to give space every side of it, but margin is only applying at top left and bottom side of table not right side.
--> for this kind of situation we have to take entire table into one div and we have to give margin to that div

task 2: 
note:   rowspan will not work if you give it into thead and expect the result in tbody also.

task 4: margin between two td is not working.

form task 4:  lables are centered justify type
            - checkboxes validation

min-width and max-width ?

css practice folder : 15_LayOut css file line no. 14  in .HeaderLinks ul....que.why we have to give overflow:hidden ?
css practice folder : 20  line 29
  .dropdown:hover .dropdown-content {
  display: block;
}
meaning of this.

css practice folder : 21 css file line 33: media query is not working.
(w3schools : image gallery topic.)

css practice : 23_My_Website: html file line 62 right-side2..and css part line 82 ...we used a position absolute and relative and top and bottom...is there any other way or is that right?


tasks :  backgrounds 

task 1: middle linewidth , bg-image pr gradients ,
task 2: opacity middle div.


query: when we give margin to the main div...scrollbar appears. solve: give padding to the body or html instead of margin.

layout task:

task 2: adhuro.
query: 02_layout.css ma margins between 2 divs in cenntered content.. in html div ni andr div levani need 6?

task 3: boxes ne only 3 side(except top) thi inset ma box-shadow aapeli 6.. how?

css practice folder 29 : css file line 153: only box5 na hover prr j animation work kre 6..box5 ne bdle background krie to nahi.
Ans--> Hover only specific div mate j work kre..let suppose aapne '.box' class vaala div na hover pr kai change kravva mangie 6ie to e change only '.box' class vaala div ni andr na elements pr j hover effect lagse. e div sivay na div ma kai change thse nai.
--> we have to use the general sibling selector(~) in this type of case. then it will work on the outer divs also.

revice tooltip in button-hover-task css file 93 no. line

task 04_overlay_on_img_Hover_solved : overlay example 1 and 2 ma text overlaying is shaking...ene vchche thi kai rite overlay aapi skay?

05_SIMPLE VEGAN WEBSITE solved ma image hover na end ma image size original aavta time prr bhi same transition effect nai lagti
->completed. done  >> jena prr hover effect mukta hoie eni css ma transition mukvanu.

css shaping image w3 schools ---> amazing css property : "shape-outside: circle(50%)"

w3schools: @property topic.

note: ::after ke ::before na content ma html ni entities aapvi hoi to hexcode k unicode j use kri skay ane e unicode pela '\' mukvo necessary chhe. ex: content: "\21D4"

about grid:

grid-template-columns : ketli columns joie 6 ena mate 
grid-template-rows : rows ni size ketla px aapvi ena maate.

gap in grid : 1. column-gap  2. row-gap  3. gap (works for to  give rest both types of gap)

query: css practice folder 41.media-query-3 ma navigation menu float vina bnavvu 6.   
ans--> give anchor tag-> display: block;
  /* veryyy veryy imp property...if we dont give this and give padding 40 or 50px then anchortag overflows the navigation bar. */

Note: text-underline-offset: 10px : css property for make space between the text and underline if we had given text-decoration: underline to that text.(09_flex_website css file line 103 (.centered_text h1))

IMP-Note: bg-image ne koi color thi aanchi bnavva ek koi div lai(ex: overlay) te div ne position:absolute aapi top left 0 and height width 100% kri ne tene bg-color aapi te color ni opacity ghtadi devi. (flex-website line 23 (.overlay))..

-->we changed here and we used gradient, bcz overlay only tyare j use krvanu jyare aapne on hover k active pr kai change krvo hoi..

--> overlay no use motabhage image pr j thy.

Note: shortcut to open the file from "files" if it is not showing the "open with code" option.
-->  go to the path of that file... write cmd and press enter...write "code ." and enter in cmd.




how to save data in local storage: E:\aryan\JQuery\Task\TO-DO_List  ---> in js file
